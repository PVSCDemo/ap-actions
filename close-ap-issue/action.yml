name: 'Close an AgilePlace Issue'
inputs:
  token:
    description: 'The secret for API access to the GH repo'
    required: true
  repository:
    required: true
  payload:
    description: 'The client_payload from AP card associated with an issue'
    required: true
runs:
  using : 'node16'
  main: 'index.js'
